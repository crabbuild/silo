[package]
name = "silo-s3-downstream-core-check"
version = "0.0.0"
edition = "2021"
rust-version = "1.94.1"
publish = false

[dependencies]
silo-s3-core = { path = "../../core", default-features = false }
