[package]
name = "prolly-s3-downstream-core-check"
version = "0.0.0"
edition = "2021"
rust-version = "1.89"
publish = false

[workspace]

[dependencies]
prolly-s3-core = { path = "../../core", default-features = false }
